import FormAuth from "../components/FormAuth";
import { LoginForm } from '../../gemini/G_FormShadTanstack';

export default function Auth() {
    return (
        <>
            <FormAuth/>
            <LoginForm/>
        </>
    );
}